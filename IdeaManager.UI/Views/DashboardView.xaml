<UserControl x:Class="IdeaManager.UI.Views.DashboardView"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:IdeaManager.UI.Views"
      mc:Ignorable="d">
    <Grid>
        <StackPanel>
            <TextBlock Text="tableau de bord" FontSize="36" FontWeight="Bold"/>
            <TextBlock Text="{Binding TotalIdeasText}"/>
            <TextBlock Text="{Binding ApprovedIdeasText}"/>
            <TextBlock Text="{Binding PendingIdeasText}"/>
            <TextBlock Text="{Binding RejectedIdeasText}"/>
        </StackPanel>
    </Grid>
</UserControl>
